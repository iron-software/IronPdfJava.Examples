IronPdf leverages the 2021 Chrome Rendering API to provide robust support for multithreading.

Different techniques for implementing threading in .NET are:

- `System.Threading.Tasks`
- `async`/`await`
- `Parallel.ForEach`